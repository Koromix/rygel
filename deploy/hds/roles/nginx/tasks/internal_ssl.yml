# This program is free software: you can redistribute it and/or modify
# it under the terms of the GNU Affero General Public License as published by
# the Free Software Foundation, either version 3 of the License, or
# (at your option) any later version.
#
# This program is distributed in the hope that it will be useful,
# but WITHOUT ANY WARRANTY; without even the implied warranty of
# MERCHANTABILITY or FITNESS FOR A PARTICULAR PURPOSE.  See the
# GNU Affero General Public License for more details.
#
# You should have received a copy of the GNU Affero General Public License
# along with this program. If not, see https://www.gnu.org/licenses/.

- name: Create local SSL folder for HDS certificate chain
  file:
    path: /tmp/hds/internal_ssl
    state: directory
  delegate_to: localhost
  become: no

- name : Create SSL configuration files
  template:
    src: '{{ item }}.j2'
    dest: '/tmp/hds/internal_ssl/{{ item }}'
  loop:
    - ssl_ca.conf
    - ssl_server.conf
  delegate_to: localhost
  become: no

- name: Generate CA and HDS certificate
  shell: |
    openssl genrsa -out /tmp/hds/internal_ssl/ca.key 4096
    openssl req -x509 -new -key /tmp/hds/internal_ssl/ca.key -config /tmp/hds/internal_ssl/ssl_ca.conf -days 372 -out /tmp/hds/internal_ssl/ca.crt
    openssl genrsa -out /tmp/hds/internal_ssl/server.key 2048
    openssl req -new -key /tmp/hds/internal_ssl/server.key -out /tmp/hds/internal_ssl/server.csr -config /tmp/hds/internal_ssl/ssl_server.conf
    openssl x509 -req -in /tmp/hds/internal_ssl/server.csr -CA /tmp/hds/internal_ssl/ca.crt -CAkey /tmp/hds/internal_ssl/ca.key -CAcreateserial -out /tmp/hds/internal_ssl/server.crt -days 372 -sha256 -extfile /tmp/hds/internal_ssl/ssl_server.conf -extensions req_ext
  delegate_to: localhost
  become: no

- name: Push root certificate to proxy
  copy:
    src: /tmp/hds/internal_ssl/ca.crt
    dest: /opt/nginx/ssl/internal/ca.crt
    owner: root
    group: root
    mode: '0600'
  delegate_to: proxy
  become: yes

- name: Push HDS certificate and key to server
  copy:
    src: '/tmp/hds/internal_ssl/{{ item }}'
    dest: '/opt/nginx/ssl/internal/{{ item }}'
    owner: root
    group: root
    mode: '0600'
  loop:
    - server.key
    - server.crt
  delegate_to: server
  become: yes

- name: Delete local SSL files
  file:
    path: /tmp/hds/internal_ssl
    state: absent
  delegate_to: localhost
  become: no
